### **扩展的基于 PIN 的认证器访问控制协议（ePACA）**

以下是对 **ePACA 协议** 的翻译与结构化解析，帮助理解其核心流程与各算法功能：

------

#### **协议组成**

ePACA 协议由 **5 个交互式算法** 构成，涉及 **客户端（C）**、**认证器令牌（T）** 和 **用户（U）** 三方协作：

| **算法**                 | **输入**                         | **输出**              | **功能**                                                     |
| ------------------------ | -------------------------------- | --------------------- | ------------------------------------------------------------ |
| **Reboot(T)**            | 令牌 T                           | -                     | 初始化令牌 T 的状态，需用户交互。必须在其他算法前执行。      |
| **Setup(T, C, U)**       | T、C、U                          | 传输记录 trans        | 用户 U 通过客户端 C 安全传输 PIN 到令牌 T。每个令牌仅执行一次。 |
| **Bind(T, C, U)**        | T、C、U                          | -                     | 在用户 U 确认下，将客户端 C 绑定到令牌 T。包含两个子算法：   |
| ↳ **Bind-C(C, U, m)**    | C、U、消息 m                     | 响应消息 m'           | 客户端 C 处理消息 m，需用户 U 确认后生成响应 m'。            |
| ↳ **Bind-T(T, m)**       | T、消息 m                        | 响应消息 m'           | 令牌 T 处理消息 m，生成响应 m'。                             |
| **Auth(C, M)**           | C、命令 M                        | 命令 M 及其授权标签 t | 客户端 C 生成命令 M 的授权标签 t，用于后续验证。             |
| **Validate(T, M, t, d)** | T、M、t、用户决策 d（接受/拒绝） | 状态（接受/拒绝）     | 令牌 T 根据用户决策 d 和标签 t 验证命令 M 的合法性。         |

------

### **流程详解**

#### **1. 令牌初始化（Reboot）**

- **触发时机**：每次令牌通电启动时。
- **用户交互**：强制用户操作（如按键），确保令牌处于活跃状态。
- **作用**：重置令牌状态，防止残留数据影响后续操作。

#### **2. PIN 安全传输（Setup）**

- **执行条件**：每个令牌仅执行一次。
- 流程：
  1. 用户通过客户端输入 PIN 码。
  2. 客户端加密传输 PIN 到令牌。
  3. 令牌存储加密后的 PIN，生成传输记录 `trans`。
- **安全目标**：防止 PIN 在传输中被窃听或篡改。

#### **3. 客户端绑定（Bind）**

- 子流程：
  - Bind-C：客户端处理绑定请求，需用户确认（如弹窗提示）。
    - **输入**：绑定请求消息 `m`。
    - **输出**：生成响应 `m'`（如加密的令牌公钥）。
  - Bind-T：令牌验证绑定请求，生成响应。
    - **输入**：客户端发送的 `m'`。
    - **输出**：绑定确认消息（如签名数据）。
- **目标**：建立客户端与令牌的信任关系，确保后续操作合法性。

#### **4. 命令授权（Auth）**

- 流程：
  1. 客户端生成操作命令 `M`（如“签名数据”）。
  2. 使用令牌公钥加密或签名，生成授权标签 `t`。
- **输出**：`(M, t)` 发送至令牌执行。

#### **5. 验证与执行（Validate）**

- 流程：
  1. 令牌接收命令 `M` 和标签 `t`。
  2. 用户决策 `d`：确认接受或拒绝操作（如输入 PIN 码）。
  3. 令牌验证 t的合法性：
     - 若验证通过且 `d = accepted`，执行命令。
     - 否则拒绝操作。
- **安全机制**：结合密码学验证与用户决策，双重保障操作安全。

------

### **关键术语**

| **术语**              | **解释**                                                  |
| --------------------- | --------------------------------------------------------- |
| **授权标签（t）**     | 由客户端生成的加密或签名数据，用于令牌验证命令合法性。    |
| **用户决策（d）**     | 用户对操作的显式确认（接受/拒绝），防止恶意命令执行。     |
| **传输记录（trans）** | Setup 阶段生成的日志，记录 PIN 传输过程，用于审计或恢复。 |
| **绑定（Bind）**      | 客户端与令牌建立信任关系的过程，确保后续通信的合法性。    |

------

### **示例场景**

#### **场景 1：安全登录**

1. **Reboot**：用户插入 YubiKey，按按钮启动令牌。
2. **Setup**：用户通过浏览器设置 PIN 码，安全传输至 YubiKey。
3. **Bind**：浏览器与 YubiKey 绑定，用户确认后生成密钥对。
4. **Auth**：用户发起登录请求，浏览器生成签名请求及授权标签。
5. **Validate**：YubiKey 验证标签并提示用户输入 PIN，通过后执行签名。

#### **场景 2：交易确认**

1. **Reboot**：启动硬件钱包。
2. **Auth**：生成转账命令 `M`（如“转账 1 BTC 至地址 X”）和标签 `t`。
3. **Validate**：硬件钱包显示交易详情，用户确认后验证标签，完成签名。

------

### **安全设计亮点**

1. **强制用户交互**：Reboot 和 Validate 阶段需用户显式操作，防止自动化攻击。
2. **一次性 Setup**：避免重复设置导致 PIN 泄露风险。
3. **双向绑定验证**：Bind 阶段确保客户端与令牌的互信关系。
4. **加密授权标签**：结合密码学与用户决策，抵御中间人攻击。

通过 ePACA 协议，认证器在 PIN 码保护和用户控制下实现安全访问控制，适用于高安全需求的场景（如金融交易、企业认证）。
### **ePlA+ePACA（WebAuthn 2 + CTAP 2.1）组合协议** 的用户认证（UA）安全模型

---

### **一、信任模型（Trust Model）**
#### **1. 继承性假设**
- **底层协议信任**：  
  继承 ePlA（WebAuthn 2）的 **auth 安全性** 和 ePACA（CTAP 2.1）的 **SUF-t' 安全性** 的所有假设。
- **首次使用信任（Trust-on-First-Use）**：  
  - **注册阶段**（ePlA 的 `Register` 和 ePACA 的 `Setup`、`Bind`）的通信是 **认证的**（如 TLS 保护）。  
  - 攻击者无法在初始设置阶段进行主动攻击（如篡改协议参数）。

#### **2. 新增假设**
- **服务器-客户端认证通道**：  
  WebAuthn 2 的认证阶段通过 TLS 等安全通道通信，防止中间人攻击。

---

### **二、预言机（Oracles）**
实验中敌手可调用以下预言机，模拟对组合协议的攻击：

#### **1. 继承的预言机**
- **来自 SUF-t' 实验**：  
  `NEWS`（创建服务器）、`NEWTPLA`（创建令牌）、`CORRUPT`（泄露令牌注册上下文）等，但排除 `AUTH` 和 `VALIDATE`。  
- **来自 auth 实验**：  
  `NEWS`、`NEWTPLA`、`CORRUPT`。

#### **2. 新增预言机**
| **预言机**                                      | **功能**                                                     |
| ----------------------------------------------- | ------------------------------------------------------------ |
| **REGISTER((S,i), (T,j,j'), (C,k), tb, UV, d)** | 模拟服务器 S 与令牌 T 通过客户端 C 的合法注册：<br>1. 执行 ePlA 的注册流程。<br>2. 调用 ePACA 的 `AUTH` 生成命令标签 `t`。<br>3. 调用 `VALIDATE` 验证标签，若失败则终止实验。 |
| **CHALLENGE((S,i), (C,k), tb, UV)**             | 模拟服务器生成挑战随机数并发送至客户端：<br>1. 生成挑战 `mrch` 和绑定状态 `tb`。<br>2. 调用 `AUTH` 生成标签 `t`，附加到输出。 |
| **RESPONSE((T,j,j'), macom, t, d)**             | 模拟令牌接收客户端消息并生成响应：<br>1. 调用 `VALIDATE` 验证标签 `t`，若失败则终止实验。 |
| **COMPLETE((S,i), macl, marsp)**                | 模拟服务器验证客户端消息和响应：<br>1. 检查是否满足 `Win-ua` 条件（见图 8）。 |

---

### **三、获胜条件（Winning Conditions）**
当 ePlA 服务器会话 `π̄_i^S` 接受客户端消息 `macl` 和响应 `marsp` 时，需满足以下所有条件：

#### **1. 会话标识符唯一性**
- **ePlA 会话**：所有非空的令牌或服务器会话标识符（`sid`）不得冲突（图 8 行 37-40）。  
- **ePACA 会话**：所有完成绑定的令牌或客户端会话 `sid` 不得冲突（图 8 行 44-45）。

#### **2. 协商内容一致性**
- **配对会话**：若令牌的注册上下文未泄露，则令牌与服务器会话的协商内容（如服务器标识符 `idS`、挑战哈希 `H(ch,tb)`、计数器 `n`）必须一致（图 8 行 42）。

#### **3. 注册阶段的合法性**
- **注册会话配对**：ePlA 的注册会话必须与 ePACA 的绑定会话配对，且授权命令和标签由未泄露的客户端生成（图 8 行 47-50）。

#### **4. 令牌响应合法性**
- **令牌归属**：已注册的令牌 `T` 必须拥有与服务器会话 `π̄_i^S` 配对的 ePlA 会话，且生成合法响应（除非注册上下文泄露）（图 8 行 52-56）。

#### **5. 用户授权验证**
- **命令授权**：令牌响应必须由 ePACA 会话验证过的授权命令生成，且用户明确批准（图 8 行 58）。

#### **6. 客户端命令来源**
- **命令绑定**：授权命令 `macom` 和标签 `t` 必须由与令牌会话配对的客户端生成，且基于服务器生成的挑战（除非客户端泄露或 PIN 泄露）（图 8 行 61-66）。

#### **7. 抗中间人攻击**
- **哈希绑定**：若哈希函数 `H` 抗碰撞，令牌与服务器会话隐式绑定令牌状态 `tb`，防止中间人篡改。

---

### **四、安全性定义（Definition 3）**
**定义 3（ePlA+ePACA 的 UA 安全性）**  
若对于所有 **PPT/QPT 敌手** `A`，其在安全参数 `λ` 下的优势：
$$
\mathrm{Adv}_{\mathrm{ePlA+ePACA,Compl}}^{\mathrm{ua}}(\mathcal{A}) = \Pr\left[\mathrm{Expt}_{\mathrm{ePlA+ePACA,Compl}}^{\mathrm{ua}}(\mathcal{A}) = 1\right]
$$
为 **可忽略函数**，则称组合协议 **ePlA+ePACA** 满足用户认证（UA）安全性。

---

### **五、安全性归约定理（Theorem 4）**
**定理 4（组合协议的 PPT/QPT 安全性）**  
若存在敌手 `A` 破坏组合协议 `Σ + Π`（即 WebAuthn 2 + CTAP 2.1）的 UA 安全性，则存在敌手 `A1` 和 `A2` 分别破坏底层协议 `Σ`（ePlA）的 auth 安全性和 `Π`（ePACA）的 SUF-t' 安全性，满足：
$$
\mathrm{Adv}_{\Sigma+\Pi,\mathrm{Compl}}^{\mathrm{ua}}(\mathcal{A}) \leq \mathrm{Adv}_{\Sigma,\mathrm{Compl}}^{\mathrm{auth}}(\mathcal{A}_1) + \mathrm{Adv}_{\Pi,\mathrm{Compl}}^{\mathrm{SUF-t'}}(\mathcal{A}_2)
$$

**含义**：组合协议的安全性优势不超过底层协议优势之和。若 WebAuthn 2 和 CTAP 2.1 各自安全，则其组合协议必然安全。

---

### **六、关键安全机制总结**
| **机制**             | **防御目标**                                                 |
| -------------------- | ------------------------------------------------------------ |
| **会话标识符唯一性** | 防止会话混淆和重放攻击（条件 1、3）。                        |
| **协商内容一致性**   | 确保服务器与令牌在未泄露状态下数据一致（条件 2）。           |
| **注册合法性验证**   | 绑定注册会话与授权命令，防止伪造注册（条件 4）。             |
| **用户显式授权**     | 令牌响应必须基于用户批准的授权命令（条件 5）。               |
| **抗中间人攻击**     | 通过哈希绑定（`H(ch,tb)`）和认证通道，确保通信完整性（条件 6、7）。 |
| **抗量子攻击**       | 若启用 `puvProtocol3`，结合后量子 KEM 和哈希函数，抵御量子计算威胁。 |

---

### **七、实际应用示例**
#### **场景 1：中间人攻击防御**
- **攻击尝试**：敌手篡改认证阶段的挑战 `ch` 或令牌状态 `tb`。  
- **防御**：哈希绑定 `H(ch,tb)` 确保篡改被检测，条件 2 和 7 触发实验终止。

#### **场景 2：令牌克隆攻击**
- **攻击尝试**：复制令牌的注册上下文（`rcT`）以伪造身份。  
- **防御**：条件 4 要求授权命令必须由未泄露的客户端生成，泄露注册上下文会导致实验终止。

---

### **八、总结**
**FIDO 2（ePlA+ePACA）** 通过分层安全模型和严格的形式化证明，实现了端到端的无密码认证安全性：  
1. **模块化设计**：WebAuthn 2 负责服务端逻辑，CTAP 2.1 管理硬件令牌交互，分工明确。  
2. **抗攻击能力**：通过唯一会话标识、哈希绑定、用户授权等多重机制，抵御中间人、重放、物理窃取等攻击。  
3. **后量子扩展**：通过 `puvProtocol3` 和参数升级，为量子计算时代提供前瞻性保护。  

详细证明步骤可参考论文 **附录 Section K**，实际部署需遵循算法选择和参数配置建议，确保理论安全转化为实践保障。 😊